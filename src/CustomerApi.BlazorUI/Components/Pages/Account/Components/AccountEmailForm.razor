@using CustomerApi.BlazorUI.Models.Account

<MudPaper Class="work-card" Elevation="0">
    <MudStack Spacing="3">
        <div>
            <MudText Class="eyebrow">Email</MudText>
            <MudText Typo="Typo.h5"><b>Trocar email</b></MudText>
        </div>

        <EditForm Model="Model" OnValidSubmit="OnValidSubmit">
            <AntiforgeryToken />
            <DataAnnotationsValidator />
            <ValidationSummary class="validation-box" />

            @if (ErrorMessages.Count > 0)
            {
                <MudAlert Severity="Severity.Error" Variant="Variant.Outlined" Class="mb-3">
                    @foreach (var error in ErrorMessages)
                    {
                        <div>@error</div>
                    }
                </MudAlert>
            }

            <MudTextField @bind-Value="Model.Email" Label="Novo email" Variant="Variant.Outlined" For="() => Model.Email" />

            <MudStack Row Justify="Justify.FlexEnd" Class="mt-4">
                <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary">
                    Atualizar email
                </MudButton>
            </MudStack>
        </EditForm>
    </MudStack>
</MudPaper>

@code {
    [Parameter, EditorRequired] public ChangeEmailRequest Model { get; set; } = new();
    [Parameter] public IReadOnlyCollection<string> ErrorMessages { get; set; } = [];
    [Parameter] public EventCallback OnValidSubmit { get; set; }
}
